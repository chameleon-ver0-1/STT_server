doctype
html
  head
    meta(charset='utf-8')
    title TEST-CHAT
  body
    div F12로 console, network 탭 확인
    script(src='/socket.io/socket.io.js')
    script.
      var socket = io.connect('ws://localhost:8005', {
        path:'/socket.io',
      });

      socket.on('news', function(data){
        console.log(data);
        socket.emit('reply', 'Hello Node.js');
      })